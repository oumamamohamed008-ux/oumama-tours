PORT=5000

DB_HOST=your-mysql-host
DB_USER=your-mysql-user
DB_PASSWORD=your-mysql-password
DB_NAME=oumama_tours
DB_PORT=3306

# Comma-separated frontend origins. Leave empty when frontend and API share one domain.
ALLOWED_ORIGINS=https://www.example.com

# Long random secret used for protected GET endpoints.
ADMIN_API_KEY=replace-with-a-long-random-secret